import React from "react";

const LogIn = () => {
  const Logout = () => {
    setuser({});
    localStorage.clear();
    window.location.reload();
  };
  return <div></div>;
};

export default LogIn;
